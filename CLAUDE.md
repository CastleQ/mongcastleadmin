# CLAUDE.md

## 프로젝트 개요
이 폴더(`C:\dev\mongcastleadmin`)에서 몽캐슬 파티룸 운영관리 admin 홈페이지를 만들어 배포까지 진행한다.

(소통/코드 작성/문제 해결 규칙은 공통 파일 `C:\Users\gjow2\.claude\CLAUDE.md`에 있음)

## 운영 정보의 원본 위치
몽캐슬파티룸 특장점·가격·주차·플랫폼 등록 현황 등 운영 정보는 **admin 페이지의 "몽캐슬파티룸 정보" 메뉴(Supabase `info_sections` 표)** 가 원본이다.
이 파일에는 더 이상 그 내용을 두지 않는다 (2026-09-13 이전에는 여기 있었음 → 창고로 이전, 수정 기록은 `info_versions`에 자동 보존).
가격·수수료 같은 운영 규칙이 코드에 필요하면 `src/lib/ledger.ts` 상수를 기준으로 한다.

## 작업 규칙 (이 프로젝트에서 실제로 겪은 실수에서 나온 것 — 반드시 지킬 것)
1. **코드·SQL 파일 수정은 Write/Edit 도구로만.** `\`, `$`, 백틱, 정규식이 들어가는 내용을 Bash `node -e`/heredoc/sed로 넣지 않는다. (셸+JS 이중 해석으로 특수문자가 깎여 5회 재작업)
2. **생성물은 검증 출력 후 넘긴다.** SQL/시드/데이터 파일을 만들면 행 수와 핵심 문구 존재 여부를 출력해 확인한 뒤 사용자에게 전달. (검증 생략으로 섹션 9개 누락)
3. **생성 스크립트는 변하지 않는 원본만 읽는다.** 곧 수정·삭제할 파일을 입력으로 쓰지 않는다. 원본은 `몽캐슬파티룸 — 특장점 정리 (v10).md`처럼 별도 파일로 둔다.
4. **SQL은 전달 전에 문장 순서·의존성을 시뮬레이션한다.** 표 → 함수/트리거 → 데이터 순서, FK가 성립하는 시점(트리거는 기본 AFTER), 대상이 없을 때도 실패하지 않는지(`drop table if exists`만 사용, 없는 표의 트리거 drop 금지). 실패해도 다시 실행 가능하게 쓴다.
5. **서버·클라이언트 공용 상수와 타입은 `src/lib/`에 둔다.** `"use client"` 파일에서는 컴포넌트만 export한다. (클라이언트 파일의 상수를 서버 컴포넌트가 import → 런타임 에러 2회)
6. **비밀·개인정보가 든 SQL(`supabase/seed.sql`, `templates*.sql`, `info*.sql`)은 git에 넣지 않는다.** `.gitignore`에 이미 등록됨.
